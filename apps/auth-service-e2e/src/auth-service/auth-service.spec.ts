/**
 * E2E tests for auth-service.
 * These tests require a running auth-service instance.
 * Run `nx serve auth-service` before executing this suite.
 */
describe('auth-service e2e', () => {
  it.todo('add integration tests (service must be running)');
});
