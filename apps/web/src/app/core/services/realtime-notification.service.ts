// Re-exported from the shared data-access lib so that app-level code that
// already imports from this path continues to work unchanged.
export { RealtimeNotificationService } from '@org/shared-data-access';
export type { RealtimeNotification } from '@org/shared-data-access';