/**
 * E2E tests for salon-service.
 * These tests require a running salon-service instance.
 * Run `nx serve salon-service` before executing this suite.
 */
describe('salon-service e2e', () => {
  it.todo('add integration tests (service must be running)');
});
