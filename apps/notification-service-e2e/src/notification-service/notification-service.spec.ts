/**
 * E2E tests for notification-service.
 * These tests require a running notification-service instance.
 * Run `nx serve notification-service` before executing this suite.
 */
describe('notification-service e2e', () => {
  it.todo('add integration tests (service must be running)');
});
