/**
 * E2E tests for review-service.
 * These tests require a running review-service instance.
 * Run `nx serve review-service` before executing this suite.
 */
describe('review-service e2e', () => {
  it.todo('add integration tests (service must be running)');
});
