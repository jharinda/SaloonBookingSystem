# This file is generated by Nx.
# Build the docker image with `npx nx docker:build api`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `nx docker:run api -p 3000:3000`.
#
FROM node:20-alpine
ENV HOST=0.0.0.0
ENV PORT=3000
WORKDIR /app
COPY apps/api/dist/ .
RUN npm ci --omit=dev
CMD ["node", "main.js"]
