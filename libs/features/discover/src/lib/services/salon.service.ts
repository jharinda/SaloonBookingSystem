/**
 * Re-export the canonical SalonService from shared-data-access.
 * Kept for backwards-compatibility with any imports pointing at this path.
 */
export { SalonService } from '@org/shared-data-access';
export type { SearchParams } from '@org/shared-data-access';
