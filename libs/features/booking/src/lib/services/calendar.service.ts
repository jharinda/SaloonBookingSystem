/**
 * Re-export the canonical CalendarService from shared-data-access.
 * Kept for backwards-compatibility with any in-feature imports.
 */
export { CalendarService } from '@org/shared-data-access';
export type { CalendarAuthUrlResponse } from '@org/shared-data-access';
